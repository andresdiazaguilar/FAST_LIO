common:
    lid_topic:  "/livox/lidar"
    imu_topic:  "/livox/imu"
    time_sync_en: false         # ONLY turn on when external time synchronization is really not possible
    time_offset_lidar_to_imu: 0.0 # Time offset between lidar and IMU calibrated by other algorithms, e.g. LI-Init (can be found in README).
                                  # This param will take effect no matter what time_sync_en is. So if the time offset is not known exactly, please set as 0.0

preprocess:
    lidar_type: 1                # 1 for Livox serials LiDAR, 2 for Velodyne LiDAR, 3 for ouster LiDAR, 
    scan_line: 6
    blind: 4

mapping:
    acc_cov: 0.1
    gyr_cov: 0.1
    b_acc_cov: 0.0001
    b_gyr_cov: 0.0001
    fov_degree:    90
    det_range:     450.0
    extrinsic_est_en:  false      # true: enable the online estimation of IMU-LiDAR extrinsic
    extrinsic_T: [ 0.04165, 0.02326, -0.0284 ]
    extrinsic_R: [ 1, 0, 0,
                   0, 1, 0,
                   0, 0, 1]

publish:
    path_en:  false
    scan_publish_en:  true       # false: close all the point cloud output
    dense_publish_en: true       # false: low down the points number in a global-frame point clouds scan.
    scan_bodyframe_pub_en: true  # true: output the point cloud scans in IMU-body-frame

pcd_save:
    pcd_save_en: true
    interval: -1                 # how many LiDAR frames saved in each pcd file;
                                 # -1 : all frames will be saved in ONE pcd file, may lead to memory crash when having too much frames.

degeneracy:
    # Probabilistic degeneracy mitigation (Week 7).  When enabled, attenuates the
    # pose columns of the LiDAR Jacobian along eigen-directions judged degenerate.
    # H_pose -> H_pose * A with  A = U P^{1/2} U^T, P = diag(p_1..p_6), p_k in [0,1].
    # With enable=false, the filter is byte-identical to baseline FAST-LIO2.
    enable: false
    probability_mode: "heuristic"   # "heuristic" | "probabilistic"
    heuristic_mode:   "soft"        # "hard" | "soft" | "sigmoid"
    hard_lambda_min_trans: 100.0
    hard_lambda_min_rot:   100.0
    soft_alpha_trans:      100.0
    soft_alpha_rot:        100.0
    sigmoid_tau_trans:     100.0
    sigmoid_tau_rot:       100.0
    sigmoid_s_trans:        50.0
    sigmoid_s_rot:          50.0
    # DRPM probabilistic mode (probability_mode: "probabilistic"):
    snr_factor:             10.0
    stdev_points:           0.02
    stdev_normals:          0.05
